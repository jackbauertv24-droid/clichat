node_modules/
# Defensive: credentials live in $XDG_CONFIG_HOME, never here, but make it
# impossible to commit one by accident.
config.json
*.token
.env
*.log

# Fetched from DeepSeek's CDN by scripts/fetch-wasm.mjs, not redistributed here.
vendor/*.wasm
