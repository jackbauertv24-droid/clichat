{
  "name": "clichat",
  "version": "0.4.0",
  "description": "Terminal client for the chat.deepseek.com web backend",
  "type": "module",
  "bin": { "clichat": "./bin/clichat.mjs" },
  "engines": { "node": ">=20" },
  "files": ["bin", "src", "scripts"],
  "scripts": {
    "postinstall": "node scripts/fetch-wasm.mjs",
    "fetch-wasm": "node scripts/fetch-wasm.mjs --strict",
    "selftest": "node bin/clichat.mjs pow-selftest",
    "test": "node --test test/*.test.mjs"
  },
  "private": true
}
